# Cloudflare secrets (set via `wrangler secret put <NAME>`)
# RESEND_API_KEY=<your-resend-api-key>

# Cloudflare vars (set in wrangler.jsonc or via `wrangler secret put`)
CONTACT_TO_EMAIL=hola@thebunker.org
RESEND_FROM="The Bunker Code <${CONTACT_TO_EMAIL}>"
ALLOWED_ORIGIN=http://localhost:4321,https://*.enlabe.workers.dev
